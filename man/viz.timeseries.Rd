% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-timeseries.R
\name{viz.timeseries}
\alias{viz.timeseries}
\title{Draw a raw point time series from an ocean cube}
\usage{
viz.timeseries(
  x,
  variable,
  longitude = NULL,
  latitude = NULL,
  depth = NULL,
  time_from = NULL,
  time_to = NULL,
  match = c("exact", "nearest"),
  tolerance = NULL,
  limits = NULL,
  na.rm = FALSE,
  points = FALSE,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{variable}{Exactly one non-empty, non-missing variable name present in
\code{x}.}

\item{longitude}{A single stored longitude. May be \code{NULL} only when the cube
contains one longitude.}

\item{latitude}{A single stored latitude. May be \code{NULL} only when the cube
contains one latitude.}

\item{depth}{A single stored depth. May be \code{NULL} only for a singleton depth
axis, including a singleton \code{NA_real_} surface depth.}

\item{time_from, time_to}{Optional scalar \code{Date} or \code{POSIXct} bounds compatible
with the cube time axis. Together they define the inclusive closed interval
\verb{[time_from, time_to]}; a \code{NULL} bound leaves that side open.}

\item{match}{Matching method passed explicitly to \code{\link[=cube_extract]{cube_extract()}}. Exact
stored-value matching is the default; \code{"nearest"} selects a stored grid
cell without interpolation.}

\item{tolerance}{Optional nearest-matching tolerance passed unchanged to
\code{\link[=cube_extract]{cube_extract()}}.}

\item{limits}{\code{NULL} or two finite numeric value-axis limits in increasing
order. Values outside the limits are squished to the scale boundary and
rows are not removed.}

\item{na.rm}{A single non-missing logical value. If \code{FALSE}, the default,
missing values remain in the plotted data and interrupt the raw line. If
\code{TRUE}, missing rows are excluded and separated observations may therefore
appear connected; values are never imputed or replaced with zero.}

\item{points}{A single non-missing logical value. If \code{TRUE}, points are drawn
over the line.}

\item{title, subtitle, caption}{Optional character scalars used as plot labels.}
}
\value{
A \code{ggplot} object. The selected variable, matched coordinates, depth,
represented time range, number of temporal rows, backend, matching settings,
and point-to-cell distance are recorded in \verb{oceancube_*} attributes.
}
\description{
\code{viz.timeseries()} selects one variable at exactly one longitude, latitude,
and depth (or the singleton surface) through \code{\link[=cube_extract]{cube_extract()}}, then draws
the stored values against time. It does not aggregate, smooth, interpolate,
impute, or otherwise transform the selected observations.
}
\details{
The simple series contract is exactly one variable, one longitude, one
latitude, one depth or surface, and multiple stored time positions. A bounded
interval is inclusive. Canonical cube time is already unique and strictly
increasing; extracted rows are plotted in stable chronological order and are
never averaged or deduplicated. POSIXct bounds use instant semantics and may
be expressed in a display timezone other than canonical UTC.

Data selection is delegated to \code{cube_extract(mode = "series")}. For a lazy
NetCDF cube, a unique bounded time selection reads only the selected point,
depth, times, and variable (or the backend's minimal physical envelope), not
the complete cube.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  values <- array(c(18, 17, NA, 15), dim = c(1, 1, 1, 4, 1))
  cube <- ocean_cube(
    lon = -79, lat = -11, depth = 0,
    time = as.Date("2020-01-01") + 0:3,
    data = values, vars = "temperature", units = "degC"
  )
  viz.timeseries(cube, "temperature")
  viz.timeseries(
    cube, "temperature",
    time_from = as.Date("2020-01-02"),
    time_to = as.Date("2020-01-03"),
    points = TRUE
  )
}
}
\seealso{
\code{\link[=cube_extract]{cube_extract()}}, \code{\link[=cube_crop]{cube_crop()}}, \code{\link[=viz.profile]{viz.profile()}}, \code{\link[=viz.transect]{viz.transect()}},
\code{\link[=viz.map]{viz.map()}}
}
