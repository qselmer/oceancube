% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-transect.R
\name{viz.transect}
\alias{viz.transect}
\title{Draw a static transect from an ocean cube}
\usage{
viz.transect(
  x,
  path,
  variable,
  time = NULL,
  depth = NULL,
  lon_col = "longitude",
  lat_col = "latitude",
  id_col = NULL,
  match = c("exact", "nearest"),
  tolerance = NULL,
  mode = c("auto", "section", "horizontal"),
  distance = c("requested", "matched"),
  limits = NULL,
  na.rm = TRUE,
  reverse_depth = TRUE,
  points = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{path}{An ordered path accepted by \code{\link[=cube_transect]{cube_transect()}}. It is passed
unchanged; \code{sf}/\code{sfc} paths are rejected by the extraction contract.}

\item{variable}{One non-empty, non-missing variable name.}

\item{time}{\code{NULL} for a singleton time axis or one stored time value.}

\item{depth}{Optional stored depth selector passed directly to
\code{\link[=cube_transect]{cube_transect()}}. A section requires at least two depths; a horizontal
transect requires exactly one physical depth or the surface depth.}

\item{lon_col, lat_col}{Column names containing path longitude and latitude.}

\item{id_col}{Optional path column used as point identifiers.}

\item{match}{Matching method passed explicitly to \code{\link[=cube_transect]{cube_transect()}}. The new
visualization API defaults to exact matching. Nearest matching may warn
when no scientific \code{tolerance} is supplied.}

\item{tolerance}{Optional nearest-matching tolerance passed unchanged to
\code{\link[=cube_transect]{cube_transect()}}.}

\item{mode}{Plot mode. \code{"auto"} chooses a section for two or more depths and
a horizontal plot for one physical depth or a surface cube.}

\item{distance}{Cumulative distance used on the x axis. \code{"requested"}, the
default, follows the user path; \code{"matched"} follows the matched grid path.
Point-to-cell \code{match_distance_km} is diagnostic and is never the x axis.}

\item{limits}{\code{NULL} or two finite numeric scale limits in increasing order.
Values outside the limits are squished to the boundary without removing
observations.}

\item{na.rm}{A single non-missing logical value. Missing values are removed
before plotting when \code{TRUE}; they are never replaced or interpolated.}

\item{reverse_depth}{A single non-missing logical value. In section mode,
\code{TRUE} places the surface at the top and greater depths downward.}

\item{points}{A single non-missing logical value. In horizontal mode, \code{TRUE}
adds points to the line; it does not alter a section plot.}

\item{title, subtitle, caption}{Optional character scalars used as plot labels.}
}
\value{
A \code{ggplot} object with the resolved variable, time, mode, distance,
depth range, backend, matching settings, maximum point-to-cell distance,
and path point count recorded in \verb{oceancube_*} attributes.
}
\description{
\code{viz.transect()} delegates ordered path matching and scientific selection to
\code{\link[=cube_transect]{cube_transect()}}, then draws either a distance-by-depth section or a
distance-by-value horizontal transect. It never interpolates values or
densifies the supplied path.
}
\details{
\code{cube_transect()} is the sole data-selection and coordinate-matching layer.
\code{viz.transect()} does not recalculate distances, access a backend reader,
aggregate duplicate cells, interpolate, or reorder the requested path.
Regular distance-by-depth grids use \code{geom_raster()} and irregular grids use
\code{geom_tile()}. Repeated path positions that create duplicate distance-depth
cells cannot define a two-dimensional section and are rejected; remove the
zero-length segment or choose the alternative distance metric when valid.
NetCDF extraction remains selective through \code{\link[=cube_transect]{cube_transect()}}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  values <- array(1:12, dim = c(3, 1, 4, 1, 1))
  cube <- ocean_cube(
    lon = c(-80, -79, -78), lat = -11, depth = c(0, 25, 50, 100),
    time = as.Date("2020-01-01"), data = values,
    vars = "temperature", units = "degC"
  )
  path <- data.frame(
    longitude = c(-80, -79, -78), latitude = rep(-11, 3)
  )
  viz.transect(cube, path, "temperature", depth = 0)
  viz.transect(cube, path, "temperature", mode = "section")
}
}
\seealso{
\code{\link[=cube_transect]{cube_transect()}}, \code{\link[=viz.map]{viz.map()}}, \code{\link[=viz.section]{viz.section()}}, \code{\link[=viz.profile]{viz.profile()}}
}
