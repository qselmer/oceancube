% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_polygon_weights.R
\name{cube_polygon_weights}
\alias{cube_polygon_weights}
\title{Build polygon-to-cube geometric weights}
\usage{
cube_polygon_weights(
  x,
  polygons,
  id_col = NULL,
  crs = NULL,
  dimension = c("2d", "3d"),
  depth_bounds = NULL,
  include_zero = FALSE
)
}
\arguments{
\item{x}{An \code{\link[=ocean_cube]{ocean_cube()}} with one-dimensional longitude and latitude axes.}

\item{polygons}{An \code{sf}, \code{sfc}, or polygonal \code{sfg} object in a geographic CRS.}

\item{id_col}{Optional column name containing feature identifiers. It is
available only when \code{polygons} is an \code{sf} object. Values and duplicates are
preserved.}

\item{crs}{CRS to assign only when \code{polygons} has no CRS. Existing geometry is
never reinterpreted.}

\item{dimension}{Generate horizontal (\code{"2d"}) or volumetric (\code{"3d"}) weights.}

\item{depth_bounds}{Explicit vertical interfaces or paired bounds for 3-D
weights. If \code{NULL}, unambiguous cube metadata is used.}

\item{include_zero}{Include all feature-cell combinations, including zero
intersections.}
}
\value{
A data frame with stable feature/cell keys, bounds, areas, fractions,
coverage diagnostics, and—when \code{dimension = "3d"}—depth and volume fields.
}
\description{
\code{cube_polygon_weights()} computes feature-specific geodesic intersections
against the rectilinear cells of an \code{\link[=ocean_cube]{ocean_cube()}}. The result is a
self-contained table of merge keys and geometric weights intended for
downstream consumers such as \code{spatind}. It does not read cube values,
aggregate variables, or calculate spatial indicators.
}
\details{
Polygon features are processed independently. Holes and multipart geometry
are preserved, and overlapping features are not unioned or normalised against
one another. Unlike \code{\link[=cube_mask]{cube_mask()}}, this function measures cell intersection
instead of classifying cell centres and does not modify cube values. It
supplies geometric inputs for downstream analysis; indicator calculation and
inference remain the responsibility of packages such as \code{spatind}.
}
