% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-section.R
\name{viz.section}
\alias{viz.section}
\title{Draw a static vertical section from an ocean cube}
\usage{
viz.section(
  x,
  variable,
  section = c("longitude-depth", "latitude-depth"),
  time = NULL,
  longitude = NULL,
  latitude = NULL,
  depth = NULL,
  limits = NULL,
  na.rm = TRUE,
  reverse_depth = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>}.}

\item{variable}{A single non-missing variable name present in \code{x}.}

\item{section}{Section orientation: \code{"longitude-depth"} or
\code{"latitude-depth"}.}

\item{time}{A single stored \code{Date} or \code{POSIXct} value. May be \code{NULL} only
when the cube has one time value.}

\item{longitude}{A single stored longitude for a latitude-depth section.
Must be \code{NULL} for a longitude-depth section and may be \code{NULL} for a
latitude-depth section only when the cube has one longitude.}

\item{latitude}{A single stored latitude for a longitude-depth section.
Must be \code{NULL} for a latitude-depth section and may be \code{NULL} for a
longitude-depth section only when the cube has one latitude.}

\item{depth}{Optional finite, unique stored depth values. \code{NULL} retains all
depths. A section must contain at least two depth levels.}

\item{limits}{\code{NULL} or two finite numeric fill-scale limits in increasing
order. Values outside the limits are squished to the scale boundary and
rows are not removed.}

\item{na.rm}{A single non-missing logical value. If \code{TRUE}, missing cells are
removed before plotting.}

\item{reverse_depth}{A single non-missing logical value. If \code{TRUE}, smaller
depth values appear at the top of the plot.}

\item{title, subtitle, caption}{Optional character scalars used as plot labels.}
}
\value{
A \code{ggplot} object with the selected variable, time, section, fixed
coordinate, depth range, and backend recorded in \verb{oceancube_*} attributes.
}
\description{
\code{viz.section()} selects one vertical plane through \code{\link[=cube_extract]{cube_extract()}} and
draws the stored horizontal-by-depth grid without interpolation or
aggregation. Longitude-depth sections fix one latitude; latitude-depth
sections fix one longitude. NetCDF inputs read only the selected plane.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  values <- array(1:12, dim = c(3, 1, 4, 1, 1))
  cube <- ocean_cube(
    lon = c(-80, -79, -78), lat = -11, depth = c(0, 25, 50, 100),
    time = as.Date("2020-01-01"), data = values,
    vars = "temperature", units = "degC"
  )
  viz.section(cube, "temperature")
}
}
\seealso{
\code{\link[=cube_validate]{cube_validate()}}, \code{\link[=cube_extract]{cube_extract()}}, \code{\link[=viz.map]{viz.map()}}
}
