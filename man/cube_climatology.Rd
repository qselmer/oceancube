% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube-climatology.R
\name{cube_climatology}
\alias{cube_climatology}
\title{Compute a recurrent climatology from an ocean cube}
\usage{
cube_climatology(
  x,
  by,
  period = NULL,
  leap = c("feb28", "drop", "keep"),
  min_n = 1L,
  diagnostics = FALSE
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{by}{A character scalar selecting \code{"day"}, \code{"month"}, or \code{"season"}.}

\item{period}{\code{NULL} for the full source range, or two ordered bounds defining
a closed reference interval. Bounds must use the same Date or POSIXct
semantics as \code{x$time}; POSIXct bounds are exact UTC instants. Intersecting
bounds outside the source range are clipped with one warning and both the
requested and effective periods are retained.}

\item{leap}{Daily leap-day policy: \code{"drop"} removes February 29, \code{"keep"}
retains it as an independent group, and \code{"feb28"} first combines February
28 and 29 within a leap year into one equally weighted February-28-equivalent
replicate. \code{leap} is not applicable to monthly or seasonal climatologies.}

\item{min_n}{A finite positive integer-like scalar giving the minimum number
of valid period-year replicates required per cell and recurrent group.}

\item{diagnostics}{A non-missing logical scalar. If \code{TRUE}, aligned
\code{n_clim_valid} and replicate-coverage arrays are retained in
\code{qa$climatology}. Sample SD is always retained independently of this flag.}
}
\value{
An in-memory \verb{<ocean_cube>}. \code{data} contains the climatological mean.
The aligned sample SD is in \code{climatology$sd}, and recurrent keys are in
\code{climatology$group_key}.
}
\description{
\code{cube_climatology()} computes a recurrent daily, monthly, or meteorological
seasonal climatology without pooling raw observations across years. Raw
finite observations are first averaged within each day-year, month-year, or
season-year replicate. The climatological mean and sample standard deviation
are then calculated across those replicates with equal replicate weights.
}
\details{
Inner period means use equal finite-observation weighting; duration weighting
is not performed. Across years or season-years, every valid replicate has one
equal weight. Irregular or gapped sampling emits at most one warning per call
and is recorded in provenance. \code{n_clim_total} counts eligible calendar-period
opportunities, \code{n_clim_valid} counts finite period-year replicates, and
\code{coverage_fraction = n_clim_valid / n_clim_total} when the denominator is
positive.

The output always contains a complete recurrent cycle. Daily keys are stable
\code{MM-DD} values: \code{drop} and \code{feb28} use the 365-day year 2001, while \code{keep}
uses leap year 2000. Monthly timestamps are the first days of 2001. Seasonal
timestamps are DJF \code{2000-12-01}, MAM \code{2001-03-01}, JJA \code{2001-06-01}, and SON
\code{2001-09-01}. These timestamps are a strictly increasing climatological
pseudo-time representation, not the historical reference period. Date input
produces Date output; POSIXct input produces UTC POSIXct midnight output.

Partial first and last periods are retained. Lazy NetCDF input is processed
with bounded source-period and spatial/depth/variable reads; neither the full
source cube nor a full chronological intermediate cube is materialized.
\code{cube_slice()} and \code{cube_crop()} deliberately discard dimensional scientific
metadata and record that action in provenance, so select or crop the source
before computing a climatology when the SD must remain attached.
}
\examples{
monthly <- ocean_cube(
  lon = -80, lat = -12, depth = 0,
  time = as.Date(c("2020-01-01", "2021-01-01")),
  data = array(c(10, 20), c(1, 1, 1, 2, 1)), vars = "temperature"
)
suppressWarnings(cube_climatology(monthly, by = "month"))

daily <- ocean_cube(
  lon = -80, lat = -12, depth = 0,
  time = as.Date(c("2020-02-28", "2020-02-29")),
  data = array(c(20, 40), c(1, 1, 1, 2, 1)), vars = "temperature"
)
cube_climatology(daily, by = "day", leap = "feb28")

seasonal <- ocean_cube(
  lon = -80, lat = -12, depth = 0,
  time = as.Date(c("2025-12-15", "2026-01-15", "2026-02-15")),
  data = array(c(3, 6, 9), c(1, 1, 1, 3, 1)), vars = "temperature"
)
cube_climatology(seasonal, by = "season")
suppressWarnings(cube_climatology(monthly, by = "month", diagnostics = TRUE))

}
\seealso{
\code{\link[=cube_aggregate_time]{cube_aggregate_time()}}, \code{\link[=clim_day]{clim_day()}}, \code{\link[=clim_month]{clim_month()}}, \code{\link[=cube_extract]{cube_extract()}}
}
