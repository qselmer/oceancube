% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_extract.R
\name{cube_extract}
\alias{cube_extract}
\title{Extract ocean-cube cells as a data frame}
\usage{
cube_extract(
  x,
  longitude = NULL,
  latitude = NULL,
  depth = NULL,
  time = NULL,
  variable = NULL,
  by = c("value", "index"),
  match = c("exact", "nearest"),
  tolerance = NULL,
  mode = c("point", "profile", "series", "table"),
  format = c("long", "wide"),
  keep_index = FALSE,
  keep_distance = FALSE
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{longitude, latitude, depth, time, variable}{Optional selectors. \code{NULL}
retains the complete axis.}

\item{by}{Whether selectors contain stored coordinate \code{"value"}s or
one-based \code{"index"} positions.}

\item{match}{Coordinate matching method. \code{"exact"} requires stored values;
\code{"nearest"} selects the nearest stored coordinate inside the domain.}

\item{tolerance}{Optional fully named list of maximum distances for nearest
matching, using numeric values for spatial/depth axes and \code{difftime} for
time.}

\item{mode}{Extraction intent: discrete \code{"point"}s, a vertical \code{"profile"},
a temporal \code{"series"}, or a general Cartesian \code{"table"}.}

\item{format}{\code{"long"} returns one row per selected array value; \code{"wide"}
returns one row per longitude-latitude-depth-time key and one column per
variable.}

\item{keep_index}{Add the selected one-based indices in the original cube.}

\item{keep_distance}{Add requested values and nearest-match distances.}
}
\value{
A base \code{data.frame} with lightweight selection and provenance
attributes. Long output contains \code{longitude}, \code{latitude}, \code{depth}, \code{time},
\code{variable}, \code{unit}, and \code{value}.
}
\description{
\code{cube_extract()} resolves the same discrete selectors as \code{\link[=cube_slice]{cube_slice()}} but
returns their Cartesian product as a base \code{data.frame} instead of creating
another cube.
}
\details{
The selectors form a Cartesian product. In long output, rows follow R array
order: longitude changes fastest, followed by latitude, depth, time, and
variable. Requested order and repeated coordinate positions are preserved.
Wide output rejects repeated variables or coordinate keys because silently
aggregating them would be ambiguous.

Nearest matching chooses a stored cell and never interpolates. With
\code{keep_distance = TRUE}, diagnostics distinguish requested values from the
selected coordinates. This option is available only with
\verb{by = "value", match = "nearest"}.
Temporal matching keeps \code{Date} civil-date semantics separate from POSIXct
instant semantics. POSIXct results remain UTC with sub-day precision.
Equidistant temporal nearest matches choose the earlier instant, and time
tolerances must be finite, non-negative scalar \code{difftime} values.

A profile requires exactly one longitude, latitude, and time. A series
requires exactly one longitude, latitude, and depth. Point and table modes do
not change the Cartesian-product semantics.

NetCDF inputs are resolved without opening the file and then read once
through the backend. Only the selected envelope and variables are read.
Extracting without selectors requests the complete cube and can create a
large table; the expected rows and approximate array bytes are calculated
before reading.
Source temporal provenance is attached to the returned table without copying
the backend's raw numeric time vector.

Unlike \code{link_events()}, which enriches independent event rows,
\code{cube_extract()} generates the Cartesian grid of its axis selectors.
\code{\link[=cube_crop]{cube_crop()}} selects continuous ranges and returns a cube; \code{\link[=cube_slice]{cube_slice()}}
selects discrete positions and returns a cube.\tabular{llll}{
   Function \tab Input \tab Semantics \tab Output \cr
   \code{cube_slice()} \tab selectors \tab Cartesian product \tab cube \cr
   \code{cube_crop()} \tab ranges \tab rectangular subdomain \tab cube \cr
   \code{cube_extract()} \tab selectors \tab Cartesian product \tab table \cr
   \code{link_events()} \tab event rows \tab row by row \tab enriched table \cr
}
}
\examples{
values <- array(seq_len(2 * 1 * 2 * 2 * 1), dim = c(2, 1, 2, 2, 1))
cube <- ocean_cube(
  lon = c(-80, -79),
  lat = -11,
  depth = c(0, 50),
  time = as.Date(c("2020-01-01", "2020-02-01")),
  vars = "temperature",
  units = c(temperature = "degC"),
  data = values
)
cube_extract(
  cube,
  longitude = -79,
  latitude = -11,
  time = as.Date("2020-02-01"),
  mode = "profile"
)
cube_extract(cube, depth = 0, mode = "table", format = "wide")
}
\seealso{
\code{\link[=cube_slice]{cube_slice()}}, \code{\link[=cube_crop]{cube_crop()}}, \code{\link[=link_events]{link_events()}}
}
