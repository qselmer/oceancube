% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_mask.R
\name{cube_mask}
\alias{cube_mask}
\title{Mask an ocean cube with polygon cell-centre coverage}
\usage{
cube_mask(
  x,
  polygons,
  crs = NULL,
  keep = c("inside", "outside"),
  boundary = c("include", "exclude")
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{polygons}{An \code{sf}, \code{sfc}, or polygonal \code{sfg} object containing only
\code{POLYGON} or \code{MULTIPOLYGON} geometries.}

\item{crs}{Optional CRS used only to assign a CRS to geometry that has none.
Geometry with an existing CRS is never reinterpreted.}

\item{keep}{Whether to retain centres \code{"inside"} or \code{"outside"} the polygon
union.}

\item{boundary}{Whether cell centres on polygon boundaries are included or
excluded.}
}
\value{
A memory-backed \verb{<ocean_cube>} with the same coordinates, units,
dimensions, and extents as \code{x}. Its \verb{<ocean_mask>} records coverage and
lightweight geometry metadata.
}
\description{
\code{cube_mask()} classifies the longitude-latitude cell centres of an
\verb{<ocean_cube>} against polygon geometry. It preserves the complete five
dimensional cube and replaces values in cells not retained by the mask with
\code{NA}.
}
\details{
Coverage is determined only from cell centres. \code{fraction_cells_kept} is a
fraction of cell centers, not a fraction of geographic area. No cell areas,
partial overlaps, interpolation, resampling, or area weighting are computed.

Several polygon features are combined as one geometric union; overlaps are
counted once and polygon holes are respected. \code{boundary = "include"} uses a
covered-by relation, while \code{"exclude"} uses strict within. Exact boundary
classification is subject to the numerical behaviour of the spatial engine.

Cubes currently have no explicit CRS field, so longitude and latitude are
interpreted as geographic EPSG:4326 coordinates. Polygon geometry must
already use a compatible geographic CRS and the same longitude convention
as \code{x$lon}. Geometry is not transformed. Dateline-crossing polygons are not
supported in this phase.

An existing compatible 3D \verb{<ocean_mask>} is combined using logical AND and
never overwritten or reactivated. Distance-to-coast metadata is preserved.
Climatology, anomaly, and dimensional QA components are discarded because
they can become stale.

A NetCDF input is read only after centre classification. For \code{keep = "inside"}, only the smallest longitude-latitude bounding rectangle containing
effective retained centres is read. \code{keep = "outside"} may require a full
spatial read in the initial implementation. Output is always fully
materialized in memory and independent of the source file.\tabular{llll}{
   Function \tab Geometry \tab Action \tab Output \cr
   \code{cube_crop()} \tab rectangular range \tab reduces axes \tab smaller cube \cr
   \code{cube_mask()} \tab polygon \tab replaces outside with \code{NA} \tab same-shape cube \cr
   future \code{cube_polygon_summary()} \tab polygon \tab summarizes values \tab table \cr
}
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  values <- array(seq_len(3 * 2), dim = c(3, 2, 1, 1, 1))
  cube <- ocean_cube(
    lon = c(-80, -79, -78), lat = c(-12, -11), depth = 0,
    time = as.Date("2021-01-01"), vars = "temperature", data = values
  )
  polygon <- sf::st_sfc(sf::st_polygon(list(matrix(
    c(-80.5, -12.5, -78.5, -12.5, -78.5, -10.5,
      -80.5, -10.5, -80.5, -12.5),
    ncol = 2, byrow = TRUE
  ))), crs = 4326)
  cube_mask(cube, polygon)
}
}
\seealso{
\code{\link[=cube_crop]{cube_crop()}}, \code{\link[=stock_mask]{stock_mask()}}, \code{\link[=crop_stock]{crop_stock()}}
}
