% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_events.R
\name{link_events}
\alias{link_events}
\title{Link gridded ocean-cube values to event points}
\usage{
link_events(
  x,
  events,
  lon_col = "lon",
  lat_col = "lat",
  date_col = "date",
  depth_col = NULL,
  vars = NULL,
  prefix = NULL,
  time_tolerance = 0L,
  keep_grid = TRUE
)
}
\arguments{
\item{x}{An \verb{<ocean_cube>} object. This can be a raw variable cube, an absolute
anomaly cube, a z-score cube, or a standardized-anomaly-magnitude cube.}

\item{events}{A data frame with event coordinates and dates.}

\item{lon_col}{Name of the longitude column in \code{events}.}

\item{lat_col}{Name of the latitude column in \code{events}.}

\item{date_col}{Name of the date or datetime column in \code{events}.}

\item{depth_col}{Optional name of the depth column in \code{events}. If \code{NULL}, the
first depth level in \code{x} is used.}

\item{vars}{Optional variables to extract. Defaults to all variables in \code{x}.}

\item{prefix}{Optional prefix for extracted columns. If \code{NULL}, columns are named
\verb{<var>_value}.}

\item{time_tolerance}{Maximum allowed date mismatch in days. Use \code{0} for exact
daily matching, or e.g. \code{1} to allow nearest day within ±1 day.}

\item{keep_grid}{Logical. If \code{TRUE}, appends matched grid lon, lat, depth, date,
and time difference.}
}
\value{
The original \code{events} data frame with extracted ocean variables appended.
}
\description{
\code{link_events()} extracts nearest-neighbour ocean values from an \verb{<ocean_cube>}
at event coordinates and dates. It is intended for linking daily anomalies to
fishing sets, lances, survey stations, biological samples, or occurrence records.
}
\section{Lifecycle}{

Deprecated as of oceancube 0.3.0. Use \code{\link[=cube_extract]{cube_extract()}} with explicit
\code{match = "nearest"} and axis-specific tolerances, applied through an explicit
event-row workflow. There is no single equivalent replacement because
\code{link_events()} performs row-wise nearest spatial matching without a public
spatial tolerance.
}

