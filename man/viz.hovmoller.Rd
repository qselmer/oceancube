% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz-hovmoller.R
\name{viz.hovmoller}
\alias{viz.hovmoller}
\title{Draw a Hovmoller diagram from one ocean-cube plane}
\usage{
viz.hovmoller(
  x,
  variable,
  axis = c("longitude", "latitude", "depth"),
  longitude = NULL,
  latitude = NULL,
  depth = NULL,
  time_from = NULL,
  time_to = NULL,
  match = c("exact", "nearest"),
  tolerance = NULL,
  limits = NULL,
  na.rm = FALSE,
  reverse_depth = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{variable}{Exactly one non-empty, non-missing variable name present in
\code{x}.}

\item{axis}{Coordinate retained with time: \code{"longitude"}, \code{"latitude"}, or
\code{"depth"}. Time is always displayed from left to right on the x axis.}

\item{longitude, latitude, depth}{Stored coordinate selectors for dimensions
not named by \code{axis}. A selector may be \code{NULL} only when that dimension is
singleton. The selector for the displayed axis must remain \code{NULL}, so the
complete stored axis is retained without hidden reduction.}

\item{time_from, time_to}{Optional scalar \code{Date} or \code{POSIXct} bounds compatible
with the cube time axis. The represented interval is inclusive and no
temporal resampling or regularization is performed.}

\item{match}{Matching method for fixed coordinates. \code{"exact"} is the
default; \code{"nearest"} selects stored coordinates without interpolation.}

\item{tolerance}{Optional nearest-matching tolerance passed unchanged to
\code{\link[=cube_extract]{cube_extract()}}.}

\item{limits}{\code{NULL} or two finite numeric fill-scale limits in increasing
order. Limits affect display only and never remove or modify values.}

\item{na.rm}{A single non-missing logical value. \code{FALSE} preserves missing
cells for explicit display; \code{TRUE} removes missing rows without filling.}

\item{reverse_depth}{A single non-missing logical value. For a time-depth
diagram, \code{TRUE} displays positive-down depth with the surface at the top.
Scientific depth values are never changed.}

\item{title, subtitle, caption}{Optional character scalars used as plot labels.}
}
\value{
A modifiable \code{ggplot} object carrying bounded \verb{oceancube_*}
attributes for the selected variable, axis, represented time range, fixed
coordinates, depth, backend, source semantics, and prepared-data kind.
}
\description{
\code{viz.hovmoller()} selects one variable over stored time and exactly one
longitude, latitude, or depth axis. Every other non-singleton dimension must
be selected explicitly. It never averages, aggregates, interpolates,
smooths, fills, calculates a climatology or anomaly, or estimates a trend.
}
\details{
The prepared data are the exact time-by-coordinate Cartesian support returned
by \code{\link[=cube_extract]{cube_extract()}}. Irregular stored spacing remains irregular and is drawn
with a deterministic display-only footprint positioned at stored coordinate
centres; subtle boundaries distinguish stored \code{NA} tiles from regions with
no stored centre. Gaps are not stretched into a continuous raster and no
scientific cell bounds are invented. The display label normalizes the
unambiguous \code{degC} alias to \code{°C}; stored unit metadata remain unchanged.
Calendar-aware non-base time axes are rejected explicitly because the D2A
ggplot renderer cannot represent them without inventing Gregorian dates.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  values <- array(1:12, dim = c(1, 1, 3, 4, 1))
  cube <- ocean_cube(
    lon = -79, lat = -11, depth = c(0, 25, 75),
    time = as.Date("2020-01-01") + c(0, 1, 3, 6),
    data = values, vars = "temperature", units = "degC"
  )
  viz.hovmoller(cube, "temperature", axis = "depth")
}
}
\seealso{
\code{\link[=cube_extract]{cube_extract()}}, \code{\link[=viz.timeseries]{viz.timeseries()}}, \code{\link[=viz.section]{viz.section()}}
}
