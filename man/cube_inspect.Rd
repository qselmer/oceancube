% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube-inspect.R
\name{cube_inspect}
\alias{cube_inspect}
\title{Inspect an ocean cube}
\usage{
cube_inspect(x, missing = c("auto", "none", "full"))
}
\arguments{
\item{x}{A valid \verb{<ocean_cube>} using the memory or NetCDF backend.}

\item{missing}{Missing-value policy. \code{"auto"} computes metrics for memory
cubes but not NetCDF cubes; \code{"none"} never computes them; and \code{"full"}
computes them for either backend. For NetCDF, \code{"full"} warns before
materializing the complete cube.}
}
\value{
A list with class \code{ocean_cube_inspection} containing structural,
coordinate, storage, missing-value, validation, and provenance summaries.
\code{time_summary} reports class, count, range, timezone, calendar, duplicate
and ordering flags, regularity, and minimum/median/maximum positive
intervals. Legacy duplicate or unsorted axes can be diagnosed without
being reordered or repaired.
}
\description{
\code{cube_inspect()} returns a compact structural and diagnostic
summary. It complements \code{\link[=cube_validate]{cube_validate()}} by describing dimensions, ranges,
resolution, estimated size, missing values, and safe provenance metadata.
}
\examples{
values <- array(c(1, NA, 3, 4), dim = c(2, 2, 1, 1, 1))
cube <- ocean_cube(
  lon = c(-80, -79), lat = c(-12, -11), depth = 0,
  time = as.Date("2020-01-01"), data = values, vars = "temperature"
)
cube_inspect(cube)
}
\seealso{
\code{\link[=cube_validate]{cube_validate()}}, \code{\link[=ocean_cube]{ocean_cube()}}
}
