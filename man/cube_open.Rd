% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cube_open.R
\name{cube_open}
\alias{cube_open}
\title{Open a local NetCDF file as a deferred ocean cube}
\usage{
cube_open(
  file,
  vars = NULL,
  lon_name = NULL,
  lat_name = NULL,
  depth_name = NULL,
  time_name = NULL,
  source = "netcdf",
  dataset_id = NULL
)
}
\arguments{
\item{file}{A single non-empty path to an existing local NetCDF file. URLs,
directories, and multiple files are not supported.}

\item{vars}{A character vector of unique data-variable names, or \code{NULL}.
\code{NULL} discovers all non-coordinate data variables from NetCDF metadata in
source order. The complete discovered set must share one compatible
rectilinear cube; incompatible variables cause an error rather than being
silently omitted.}

\item{lon_name, lat_name, depth_name, time_name}{Optional explicit physical
dimension names. Explicit mappings take precedence over the backend's CF
attribute evidence and known-name fallback.}

\item{source}{A non-empty source label stored with ingestion metadata.}

\item{dataset_id}{An optional non-empty dataset identifier.}
}
\value{
An \code{ocean_cube} with a read-only NetCDF storage backend and no
materialized \code{data} component.
}
\description{
\code{cube_open()} is an experimental source-opening API in the oceancube 0.3.0
development cycle. It opens one existing local NetCDF file as a read-only
deferred NetCDF backend. Construction reads structural metadata and the
coordinate values required by the canonical cube, but it does not read
scientific variable arrays or create \code{x$data}.
}
\details{
The returned object has the usual \code{c("ocean_cube", "list")} class. Its
internal, versioned \code{x$storage} descriptor records how bounded I/O can be
performed. The descriptor schema is implementation metadata and is not a
stable public API. Independently, \code{x$metadata$cf} preserves the source CF
metadata in a backend-independent internal schema. No NetCDF connection is
retained in either object: each
scientific read validates the source, opens it, reads the required block,
and closes it.

This is deferred I/O with bounded reads for supported operations, not a
lazy computation graph. Use \code{\link[=cube_collect]{cube_collect()}} when an independent in-memory
cube is required. A serialized deferred cube remains usable while its source
exists at the same path with the same size, modification time, and compatible
physical schema. Moving, deleting, or changing that source causes a
deterministic error when an operation next needs it.
}
\examples{
\dontrun{
x <- cube_open("ocean.nc", vars = "sst")
cube_inspect(x)

small <- cube_crop(
  x,
  longitude = c(-80, -78),
  latitude = c(-12, -10)
)

values <- cube_extract(
  x,
  longitude = -79,
  latitude = -11,
  match = "nearest",
  mode = "series"
)

mem <- cube_collect(x)
}

}
\seealso{
\code{\link[=read_nc]{read_nc()}}, \code{\link[=cube_collect]{cube_collect()}}, \code{\link[=cube_inspect]{cube_inspect()}}, \code{\link[=cube_crop]{cube_crop()}},
\code{\link[=cube_extract]{cube_extract()}}
}
